# Changelog

本项目遵守 [Semantic Versioning](https://semver.org/)。

## [1.5.5] - 2026-08-26

### 变更

- `sprite_tools.py` 的 `--icon` / `icon` 参数改为**为每张图各生成一个图标候选**（`{name}_icon.png`），不再是仅取第一张生成单一 `icon.png`——便于用户预览对比后挑选最合适的一张作为最终图标（选定后改名 `icon.png` 即可）；图标基于该图基准稿预乘缩放，不重复抠图

## [1.5.4] - 2026-08-26

### 移除（破坏性）

- 删除 `preprocess.py` / `preprocess2.py` 薄封装脚本：`sprite_tools.py` 一条命令即可覆盖原两步流程（见 README「更换形象」）。有旧流程脚本习惯的用户请改用：
  `python sprite_tools.py --src raw_sprites --out sprites --names 正面,侧面,背面 --height 340 --sizes 187,238,306 --icon 64`

## [1.5.3] - 2026-08-26

### 新增

- **sprite_tools.py 独立精灵图工具箱**：将 preprocess.py / preprocess2.py 的核心算法（白底泛洪抠图、边缘去污、预乘 alpha 缩放）封装为单文件、零项目依赖（仅 Pillow）的可复用模块——拷贝到其他项目即可 `import` 或命令行使用；支持自动扫描目录、任意文件名/尺寸组合、可选图标生成；`preprocess*.py` 改为基于它的薄封装（CLI 行为不变）
- 工具箱单元测试 11 个（合成白底图验证，不依赖真实素材），需 Pillow、缺失自动跳过

## [1.5.2] - 2026-08-26

### 修复

- **喂食面板与台词气泡重叠**：双击弹出喂食面板时立即清空当前气泡（两者默认位置上下重叠），投喂后的新台词正常显示

### 变更

- **右键菜单重组**：新增「设置」二级菜单收纳「设置 Key / 设置城市 / 监控间隔」三个低频配置项，一级菜单更精简（高频开关项如深度思考/代理/系统监控保持在一级）

## [1.5.1] - 2026-08-26

### 修复

- **天气播报中英文粘连/泄漏英文**：原天气映射表仅覆盖 7 种天气，wttr.in 实际返回 40+ 种（如 "Patchy rain nearby"），未命中的英文原样混入中文句子。现以数字 `weatherCode` 码表为主（补全全部 45 种 WWO 天气码中文），英文描述表为次级回退，仍未命中时在中英文边界自动补空格兜底；温度显示优化为「26°C」并在数字前后加空格（`深圳今天 30°C，天气局部有雨`）

## [1.5.0] - 2026-08-26

### 新增

- **流式回复**：AI 对话改用 `stream: true`，回复逐段显示在气泡上（每 4 字刷新），不再干等整句生成完；思考段（`reasoning_content`）不上屏，等待期仍有 💭 指示
- **聊天记录窗口**：右键菜单「聊天记录」可回看全部历史（自动滚到最新）、一键清空；历史持久化到 `chat_history.json`，重启后上下文自动恢复（不入仓库）
- **指数退避重试**：429/5xx 自动重试（最多 3 次，0.5s/1s 退避），网络异常与 4xx 不重试直接报错
- **GPU 多卡监控**：枚举全部 NVIDIA 显卡，按最高温度告警（单卡失败不影响其余）
- **build.bat**：本地一键打包 exe（自动装 dev 依赖 + PyInstaller）
- **pytest-qt 焦点回归测试**：锁定 v1.1.2 修复的「对话框键盘无效」Bug（PySide6/pytest-qt 缺失时自动跳过）

### 变更

- **性能**：气泡换行结果缓存，驻留期间不再每个 20ms tick 重复逐字测量
- `say()` 新增 `force` 参数（流式增量刷新绕过去重）

## [1.4.0] - 2026-08-26

### 新增

- **系统监控可配置**：
  - 右键菜单「系统监控」开关（`monitor_enabled`，**默认关闭**，开启后立即检测一次）
  - 右键菜单「监控间隔」手动修改检测周期（`monitor_interval_s`，默认 10 秒，范围 5~3600 秒，手改配置文件越界/非法值自动钳制或回退默认）
  - 监控告警气泡同样按字数动态延长停留时间

### 破坏性变更

- 系统监控由「始终开启」改为**默认关闭**：从旧版本升级的用户如需监控，请在右键菜单手动开启

## [1.3.0] - 2026-08-26

### 变更

- **取消 30 字硬截断**：AI 回复超出 Prompt 约束的 25 字时不再被切断（原实现截断丢内容、历史也只存半句，体验差）。气泡本身支持自动换行，现完整显示回复，并按字数动态延长停留时间（基础 2.8s + 每字 0.12s，封顶 12s）；对话历史同步保存完整回复，上下文质量更好
- 仅保留 120 字极端上限（防模型失控刷屏），正常回复永远不会触达

## [1.2.0] - 2026-08-26

### 新增

- **等待回复视觉反馈**：AI 处理期间（含深度思考）头顶显示 💭 动态省略号气泡，不再"疑似卡死"
- **Key 即时验证**：「设置 Key」保存后立刻后台 ping 一次，当场气泡反馈 Key 是否有效
- **代理开关**：右键菜单「使用系统代理」（默认开启）。被代理/VPN 拦截导致超时时可关闭走直连；对 DeepSeek 与天气查询同时生效（`use_proxy` 存于 config.json）

### 变更

- **连接复用**：`DeepSeekClient` 持有 `requests.Session` 复用 TCP/TLS 连接，主窗口按（key/thinking/代理）签名缓存客户端，省去每次握手延迟；天气查询同样改用模块级 Session

## [1.1.2] - 2026-08-26

### 修复

- **聊天框键盘焦点失效**：无边框 Tool 窗口 `show()` 后不会成为系统活动窗口，键盘输入进不了输入框、需多次点击才生效。现 `popup_at` 显式 `activateWindow()` + `setFocus(ActiveWindowFocusReason)`
- **AI 无回复难排查**：
  - 超时/连接失败原先只弹 2.8 秒气泡且不写日志（用户根本看不到）；现错误气泡延长至 8 秒、错误文本从 12 字放宽到 40 字，并全部写入 `logs/pet.log`
  - API 非 200 响应体现在完整记录到日志（前 300 字符）

### 新增

- 右键菜单「测试DS连接」：真实发一条消息，结果以持久弹窗展示（成功含耗时与回复，失败含排查建议），不再依赖一闪而过的气泡

## [1.1.1] - 2026-08-25

### 变更

- 默认模型 `deepseek-v4-pro` → `deepseek-v4-flash`（响应结构与 V4 Pro 一致，解析逻辑无需调整：普通模式也可能返回 `reasoning_content`，已按既定逻辑跳过）

## [1.1.0] - 2026-08-25

### 变更

- DeepSeek 接口迁移至 V4 Pro 规范：
  - 端点 `https://api.deepseek.com/v1/chat/completions` → `https://api.deepseek.com/chat/completions`（去掉 `/v1` 前缀）
  - 模型 `deepseek-chat` → `deepseek-v4-pro`
  - 请求体显式携带 `stream: false`

### 新增

- 「深度思考」开关（右键菜单，存于 `config.json` 的 `ds_thinking`，默认关闭）：
  - 开启时发送 `thinking: {"type": "enabled"}` + `reasoning_effort: "high"`，不设 `max_tokens`（避免推理 token 耗尽 100 上限导致空回复）、不发送 `temperature`，超时放宽至 60 秒
  - 关闭时 `thinking: disabled`，保持原短平快参数（`max_tokens=100` + `temperature=0.9`）
- 响应解析跳过 `reasoning_content`（思考过程不上屏），`content` 为空时明确报错

## [1.0.0] - 2026-08-25

结构化重构版本，功能与原版保持一致，另补全进食动画。

### 新增

- 标准包结构 `dafeiyu_pet/`（services / ui 分层），取代 1000+ 行单文件
- pytest 单元测试（配置 / 台词 / 朝向逻辑 / AI 消息构建 / 天气解析 / 监控阈值）
- ruff lint + GitHub Actions CI（lint、测试、手动触发的 exe 打包）
- 滚动文件日志 `logs/pet.log` 替代 print 调试输出
- `pyproject.toml` 项目元数据与版本号，依赖加上版本上限
- 双击喂食真正播放进食挤压/咀嚼动画（原 `eat_t` 赋值后从未使用）
- 右键菜单新增「设置城市」（原先只能手改 config.json）
- 天气查询移入后台线程，不再卡住 UI 最多 10 秒
- `CHANGELOG.md`

### 变更

- 文件全部改英文名：`桌宠.py` → 包结构，`启动桌宠.bat` → `start_pet.bat`（纯 ASCII，修复 GBK 编码乱码问题，并增加 Python 缺失提示）
- 配置**修改即落盘**（原版仅退出时保存，崩溃丢失全部改动）
- API Key 输入改为密文回显（防肩窥）
- `preprocess*.py` / `make_zip.py` 参数化（argparse），去除硬编码 `D:\` 个人路径
- 城市名 URL 编码后再请求 wttr.in
- 精灵图缺失时启动报明确错误（原为静默空白窗口）
- `.gitignore` 不再忽略 `*.spec`，打包配置入库

### 修复

- 删除死代码：未使用的 `load_config()`、注释掉的 IP 定位、无用状态变量
- 裸 `except:` 改为 `except Exception` 并记录日志
- `check_system_status` 方法体错误缩进
- `eat_t` 未初始化即可能被引用的隐患
- DeepSeek 网络异常分类（超时/连接失败/API 错误），工作线程兜底日志
