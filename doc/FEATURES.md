# 大肥鱼桌宠 —— 功能清单

> 用途：全量功能盘点（含入口、配置键、引入版本），作为需求基线与同类项目生成的功能模板。
> 配套文档：[ARCHITECTURE.md](ARCHITECTURE.md)（结构）、[DESIGN.md](DESIGN.md)（决策理由）。
> 对应版本：v1.5.2（2026-08）。「版本」列为功能引入/定型版本，见 CHANGELOG.md。

## 1. 桌宠本体

| 功能 | 说明 | 操作入口 | 引入版本 |
|------|------|----------|----------|
| 三视图行走 | 左右走用侧面（自动镜像）、上走用背面、下走用正面；1.15 倍水平偏向系数 | 自动 | v1.0（重构基线） |
| 自由散步模式 | 屏内随机目标点行走，到达后休息 8~18s | 右键「模式 ▸ 自由散步」 | v1.0 |
| 跟随鼠标模式 | 距鼠标 100px 内停下等待，超出则跟随 | 右键「模式 ▸ 跟随鼠标」 | v1.0 |
| 原地待着模式 | 不移动，只做待机小动作 | 右键「模式 ▸ 原地待着」 | v1.0 |
| 拖拽 | 按住左键移动；侧身朝向拖动方向；松手 50% 概率说话 | 左键按住拖动 | v1.0 |
| 单击互动 | 70% 蹦跳 + 60% 回嘴台词 + 弹出 🗨️ 功能面板 | 左键单击 | v1.0 |
| 双击喂食 | 弹出喂食面板（🐟🍰🍭🍡💎），投喂触发进食挤压/咀嚼动画 + 台词 | 左键双击 | v1.0（动画 v1.0 补全） |
| 三档大小 | 小 0.55 / 中 0.7 / 大 0.9 倍精灵 | 右键「大小 ▸」 | v1.0 |
| 呼吸/摇摆动画 | 待机正弦微动 + 行走摇摆起伏 | 自动 | v1.0 |
| 转向交叉淡化 | 换朝向时新旧精灵 15%/帧 淡入淡出 | 自动 | v1.0 |
| 加减速惯性 | 速度按 0.3 系数缓动，非瞬时启停 | 自动 | v1.0 |
| 待机小动作 | 随机蹦跳/摇摆/伸懒腰/说话（各概率分支） | 自动 | v1.0 |
| 台词系统 | 日常 13 句 / 回嘴 7 句 / 思维链心声 8 句（灰色斜体）/ 拖拽 4 句 / 喂食按食物分组；社区梗取材 | 自动 | v1.0 |
| 说话冷却 | 日常台词 1500 tick（30s）内不重复触发 | 自动 | v1.0 |

## 2. 气泡系统（桌宠的"嘴"）

| 功能 | 说明 | 引入版本 |
|------|------|----------|
| 自动换行 | 按像素宽逐字断行（中文友好），最大 240px | v1.0 |
| 动态停留时长 | 基础 2.8s + 每字 0.12s，封顶 12s | v1.3.0 |
| 台词去重 | 同句不连播；`force` 参数供流式刷新绕过 | v1.5.0 |
| 错误气泡加长 | 错误类气泡固定 8s（普通 2.8s 一闪而过） | v1.1.2 |
| 💭 思考指示 | AI 处理期间头顶动态省略号（其他气泡优先） | v1.2.0 |
| 换行缓存 | 气泡驻留期间缓存断行结果，tick 不重复测量 | v1.5.0 |
| 气泡互斥 | 喂食面板弹出前清空同位置气泡 | v1.5.2 |

## 3. AI 对话（DeepSeek）

| 功能 | 说明 | 操作入口 | 配置键 | 引入版本 |
|------|------|----------|--------|----------|
| AI 聊天 | deepseek-v4-flash，端点无 `/v1` 前缀；人设贱兮兮但可爱 | 单击 → 🗨️ → 输入框 | `ds_api_key` | v1.1.0 |
| 流式回复 | `stream:true`，每 4 字增量刷新气泡，不等整句生成 | — | — | v1.5.0 |
| 深度思考 | thinking=enabled + reasoning_effort=high；不设 max_tokens/temperature（防推理耗尽致空回复）；超时 60s | 右键「深度思考 ☐」 | `ds_thinking`（默认关） | v1.1.0 |
| 回复不截断 | 仅 120 字极端上限防失控；长回复完整换行显示 | — | — | v1.3.0 |
| 对话历史 | 最近 40 条上下文；持久化 `chat_history.json` 重启恢复；损坏容错 | — | — | v1.0 / 持久化 v1.5.0 |
| 聊天记录窗口 | 回看全部历史（自动滚到最新）/ 一键清空 | 右键「聊天记录」 | — | v1.5.0 |
| Key 管理 | Password 密文回显；保存后后台 ping 即时验证（气泡反馈有效/失败原因） | 右键「设置 ▸ 设置 Key」 | `ds_api_key` | v1.0 / 验证 v1.2.0 |
| 连接诊断 | 真实发一条消息，持久弹窗展示耗时/回复或失败原因 + 排查建议 | 右键「测试DS连接」 | — | v1.1.2 |
| 自动重试 | 仅 429/5xx 指数退避（0.5s/1s，共 3 次）；4xx/网络异常快速失败 | — | — | v1.5.0 |
| 连接复用 | requests.Session 复用 TCP/TLS；客户端按 (key,thinking,proxy) 签名缓存 | — | — | v1.2.0 |
| 代理开关 | 关闭即 `trust_env=False` 直连，绕过代理/VPN 拦截 | 右键「使用系统代理 ☐」 | `use_proxy`（默认开） | v1.2.0 |
| 聊天暂停移动 | 聊天框/面板打开期间桌宠停止移动，呼吸动画照常 | — | — | v1.0 |
| 键盘焦点 | 弹出即 activateWindow + setFocus，免点击直接打字 | — | — | v1.1.2（Bug 修复） |

## 4. 天气查询

| 功能 | 说明 | 操作入口 | 配置键 | 引入版本 |
|------|------|----------|--------|----------|
| 天气播报 | wttr.in j1 接口，后台线程查询不卡 UI；「深圳今天 30°C，天气局部有雨」 | 右键「查看天气」 | `city`（默认深圳） | v1.0 |
| 城市设置 | 输入框修改播报城市 | 右键「设置 ▸ 设置城市」 | `city` | v1.0（菜单化） |
| 中文码表 | 数字 weatherCode 主映射（45 种 WWO 码全量）→ 英文描述表次级 → 原文中英文边界自动补空格兜底 | — | — | v1.5.1 |
| 代理联动 | 天气请求同样遵循「使用系统代理」开关 | — | `use_proxy` | v1.2.0 |

## 5. 系统监控（默认关闭）

| 功能 | 说明 | 操作入口 | 配置键 | 引入版本 |
|------|------|----------|--------|----------|
| 监控开关 | 默认关闭；开启立即检测一次 | 右键「系统监控 ☐」 | `monitor_enabled`（默认 false） | v1.0 / 默认关 v1.4.0 |
| CPU 告警 | ≥ 90% 气泡提醒 | — | — | v1.0 |
| 内存告警 | ≥ 95% 气泡提醒 | — | — | v1.0 |
| GPU 温度告警 | NVIDIA 多卡取最高温，> 80°C 提醒；pynvml 缺失优雅降级；单卡失败不影响其余 | — | — | v1.0 / 多卡 v1.5.0 |
| 间隔可调 | 默认 10s，范围 5~3600s；手改配置文件越界/非法值自动钳制或回退 | 右键「设置 ▸ 监控间隔」 | `monitor_interval_s` | v1.4.0 |

## 6. 系统集成

| 功能 | 说明 | 操作入口 | 配置键 | 引入版本 |
|------|------|----------|--------|----------|
| 系统托盘 | 图标常驻；左键托盘=显示/隐藏；右键=完整同款菜单 | — | — | v1.0 |
| 窗口置顶 | 无边框 Tool 窗口不占任务栏 | 右键「窗口置顶 ☐」 | `topmost`（默认开） | v1.0 |
| 鼠标穿透 | Win32 WS_EX_TRANSPARENT；穿透后从托盘菜单解除 | 右键「鼠标穿透 ☐」 | `passthrough` | v1.0 |
| 开机自启 | Startup 目录建 .lnk（PowerShell 创建）；路径解析兼容源码/venv/exe | 右键「开机自启 ☐」 | `autostart` | v1.0 |
| 显示/隐藏 | 托盘左键或菜单切换 | 右键「显示/隐藏」 | — | v1.0 |
| 回到屏幕内 | 分辨率变化后拉回可视区 | 右键「回到屏幕内」 | — | v1.0 |
| 位置记忆 | 关闭时保存坐标，重启恢复；启动时钳制回屏幕 | — | `x` / `y` | v1.0 |
| 配置即时落盘 | 任何修改立即写 `config.json`，崩溃不丢；未知字段向前兼容 | — | — | v1.0（重构） |

## 7. 工程与开发者功能

| 功能 | 说明 | 入口 | 引入版本 |
|------|------|------|----------|
| 滚动文件日志 | logs/pet.log，512KB×3 份滚动；pythonw 无控制台时唯一取证渠道 | 自动 | v1.0 |
| 单元测试 | 66 个用例：配置/逻辑/AI 契约/SSE/重试/天气/监控/GUI 焦点回归（缺依赖自动跳过） | `pytest` | v1.0 |
| Lint | ruff（E/F/W/B/UP/SIM，行宽 100） | `ruff check .` | v1.0 |
| 一键打包 | build.bat 自动装 dev 依赖 + PyInstaller | `build.bat` | v1.5.0 |
| 打包配置 | dafeiyu_pet.spec：单文件 windowed、collect_all 收集三方数据文件、icon | `pyinstaller --noconfirm dafeiyu_pet.spec` | v1.0 |
| 源码分享包 | make_zip.py 白名单制打包，天然排除 config/历史/日志等本地数据 | `python make_zip.py` | v1.0 |
| 素材预处理 | **sprite_tools.py 独立工具箱**（单文件零项目依赖，仅 Pillow，可拷贝到任意项目复用）：白底泛洪抠图 / 边缘去污 / 预乘 alpha 缩放 / 批量目录流水线，库 + CLI 双形态；preprocess.py / preprocess2.py 为本项目薄封装 | `python sprite_tools.py --help` | v1.5.3 |
| 多形态路径 | 源码 / pip 安装 / PyInstaller frozen 三形态自动适配数据与资源目录 | 自动 | v1.0 |
| 启动脚本 | start_pet.bat：纯 ASCII + CRLF，自动选 venv/系统 Python，缺 Python 有提示 | 双击 | v1.0 |

## 8. 配置键总表（config.json schema）

| 键 | 类型 | 默认 | 说明 |
|----|------|------|------|
| `mode` | str | `"wander"` | wander / follow / still，非法值回退默认 |
| `size` | float | `0.7` | 精灵倍率（0.55/0.7/0.9） |
| `topmost` | bool | `true` | 窗口置顶 |
| `passthrough` | bool | `false` | 鼠标穿透 |
| `autostart` | bool | `false` | 开机自启 |
| `x` / `y` | int\|null | `null` | 窗口位置记忆 |
| `ds_api_key` | str | `""` | DeepSeek API Key（密文输入，本地保存） |
| `ds_thinking` | bool | `false` | 深度思考模式 |
| `use_proxy` | bool | `true` | 系统代理（false = 直连） |
| `monitor_enabled` | bool | `false` | 系统监控开关 |
| `monitor_interval_s` | float | `10.0` | 监控间隔（5~3600，越界钳制） |
| `city` | str | `"深圳"` | 天气城市 |

> 未列出的字段读取时保留、写入时原样带出（向前兼容）。

## 9. 生成同类项目的功能模板建议

按优先级实现（P0 = 最小可用产品）：

- **P0 骨架**：透明无边框窗口 + 定时器主循环 + 托盘 + 右键菜单 + 配置落盘 + 拖拽/单击/双击交互 + 气泡（换行/动态时长）+ 文件日志
- **P1 生命感**：多视图/朝向镜像 + 动画状态机（三角函数系） + 台词系统 + 交互回嘴
- **P2 智能**：AI 客户端（Session 复用/异常分类/重试/流式）+ Key 管理（密文/即时验证/诊断）+ 历史持久化
- **P3 工具**：天气（码表回退链）+ 系统监控（默认关、间隔可调）
- **P4 分发**：PyInstaller spec + build 脚本 + 白名单分享包 + 素材预处理脚本 + 测试分层

## 10. 规划中功能（Roadmap，v1.5.2 尚未实现）

> 以下为已确认的后续需求，设计定稿但代码未开发；配置键为规划值，当前版本 config.json 中不存在。

### 10.1 多服务商 AI 接入（OpenAI / Anthropic 兼容）

| 规划项 | 说明 |
|--------|------|
| 协议支持 | OpenAI 兼容协议（DeepSeek-v4-pro、GLM-5.3、Kimi-K3、MiniMax-M3 等）+ Anthropic 原生 Messages 协议 |
| 服务商预设 | 内置预设一键切换（自动填 base_url / 默认模型 / 协议），另留「自定义」入口 |
| 用户可配置 | 右键「设置 ▸ AI 服务」对话框：服务商预设、接口地址、模型名、协议、密钥 |
| 规划配置键 | `ai_provider`（预设名）、`ai_base_url`、`ai_model`、`ai_protocol`（openai/anthropic）、`ai_thinking` |
| 兼容迁移 | 旧键 `ds_api_key` / `ds_thinking` 启动时自动迁移为 `ai_*` 等价配置 |
| 架构影响 | `services/deepseek.py` 演化为 `services/ai.py`：请求体构建 / 响应解析 / 流式解析按协议适配，客户端、重试、历史层不变 |

### 10.2 窗口半透明设置

| 规划项 | 说明 |
|--------|------|
| 透明度档位 | 右键「透明度 ▸」：100% / 80% / 60% / 40% 四档（默认 100%） |
| 规划配置键 | `window_opacity: 1.0`，即时生效并落盘记忆 |
| 目的 | 工作时降低桌宠对桌面内容的遮挡；档位化而非滑杆，与「大小」菜单交互一致 |
| 实现注意 | 优先仅对精灵本体降 alpha（paintEvent 内 `setOpacity`），保持气泡文字完全不透明可读；`setWindowOpacity` 会连带气泡一起变淡，仅作整窗降档的备选 |

### 10.3 密钥加密存储

| 规划项 | 说明 |
|--------|------|
| 加密方案 | Windows DPAPI（`CryptProtectData`，绑定当前用户），密文 base64 存 config.json，带 `enc:` 前缀标识 |
| 明文禁令 | config.json 中不再出现任何明文 API Key；手改配置写入明文时启动自动加密重写（迁移） |
| 边界说明 | DPAPI 防的是「配置文件被拷走/分享泄露」，同用户下的进程仍可解密——桌面应用合理的威胁模型，需在文档如实声明 |
| 架构影响 | 新增 `services/crypto.py`（protect/unprotect 纯封装 + 可测 fake），读写密钥统一走该模块 |
